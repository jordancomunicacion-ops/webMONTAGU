server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Handle TikTok trailing slash for .txt files
    # This allows /filename.txt/ to serve /filename.txt
    location ~* ^/(.*\.txt)/$ {
        root /usr/share/nginx/html;
        rewrite ^/(.*\.txt)/$ /$1 break;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
